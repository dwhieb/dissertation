% The packages, settings, and commands in this file are only used for development
% not the final dissertation document

% PACKAGES

% NB: This line needs to come before the todonotes package is loaded
\setlength{\marginparwidth}{1in}

\usepackage[
  backgroundcolor=cyan,
  colorinlistoftodos,
  linecolor=cyan,
  obeyDraft,
  textsize=footnotesize
]{todonotes}

% NB: This needs to be declared *after* the todonotes package
\reversemarginpar

% COMMANDS

% \addref{}
%
% Add a placeholder citation
%
% Example:
%
% Is is the subject of much debate whether these language-specific categories can be mapped onto each other or compared in any useful way \addref{Haspelmath 2010, other publications on this debate}.
%
\newcommand{\addref}[1]{%
  \colorbox{magenta}{(CITE: #1)}%
  \todo[
    caption={citation},
    color=magenta,
    noline,
    size=\tiny
  ]{}
}
