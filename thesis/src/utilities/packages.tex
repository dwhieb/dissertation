% This file declares all packages used in the thesis.
% Packages which require configuration upon initialization are configured here.
% Configuration for packages which does not happen upon initialization happens in dissertation.sty.

\usepackage{booktabs}
\usepackage{calc}
\usepackage{caption}
\usepackage{csquotes}
\usepackage{enumitem}
\usepackage{etoolbox}
\usepackage{fancyhdr}
\usepackage{fontspec}
\usepackage{ifdraft}
\usepackage{imakeidx}
\usepackage{longtable}
\usepackage{multicol}
\usepackage{multirow}
\usepackage{setspace}
\usepackage{tabu}
\usepackage{tabularx}
\usepackage{titlesec}
\usepackage{titling}
\usepackage{xcolor}
\usepackage{xstring}

% enable linguistic examples
\usepackage{gb4e} % must be loaded late to avoid issues with underscores
\noautomath       % avoids subsequent issues with gb43 redefining the underscore

% set citation options
\usepackage[
  backend      = biber,
  maxbibnames  = 99,
  maxcitenames = 3,
  style        = utilities/unified
]{biblatex}

% set page dimensions
\usepackage[
  top    = 1in,
  bottom = 1in,
  left   = 1.25in,
  right  = 1in,
]{geometry}

% enable embedded graphics
\usepackage[final]{graphicx}

% needs to be loaded after graphicx
\usepackage{pdflscape}

% enable superscripts
\usepackage[super]{nth}

% "titles" option makes hyperlinks to ToC, LoF, LoT work correctly
\usepackage[titles]{tocloft}

% NB: Best practice is to import the hyperref package last
\usepackage{hyperref}
